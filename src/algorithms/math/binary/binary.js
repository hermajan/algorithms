/**
 * Converts decimal number to binary using string.
 * @param {type} number Number to convert.
 * @return {String} String with binary number.
 */
function dec2bin(number) {
  return new Number(number).toString(2);
}
